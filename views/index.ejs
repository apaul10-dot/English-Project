<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hunger Stoppers</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #e0f7fa 0%, #e3f2fd 100%);
        }
        .navbar {
            width: 100%;
            background: #1a73e8;
            color: white;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            padding: 0.5rem 2rem;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
            height: 64px;
            box-shadow: 0 4px 16px rgba(26, 115, 232, 0.08);
        }
        .navbar-title {
            font-size: 1.7rem;
            font-weight: 700;
            letter-spacing: 2px;
            margin-right: 3rem;
        }
        .navbar-links {
            display: flex;
            gap: 2rem;
            align-items: center;
            margin-right: auto;
        }
        .navbar-link {
            color: white;
            text-decoration: none;
            font-size: 1.1rem;
            font-weight: 500;
            padding: 0.3rem 0.7rem;
            border-radius: 4px;
            transition: background 0.2s, color 0.2s;
        }
        .navbar-link:hover, .navbar-link.active {
            background: #1565c0;
            color: #b2ebf2;
        }
        .main-content {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            height: 100vh;
            padding-top: 64px;
            box-sizing: border-box;
        }
        .welcome-message {
            margin-left: 7vw;
            background: white;
            padding: 2.5rem 3.5rem;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(26, 115, 232, 0.10);
            font-size: 2.2rem;
            color: #1a73e8;
            font-weight: 700;
            max-width: 500px;
        }
        @media (max-width: 700px) {
            .main-content {
                justify-content: center;
                padding-top: 80px;
            }
            .welcome-message {
                margin-left: 0;
                padding: 1.5rem 1rem;
                font-size: 1.3rem;
            }
            .navbar {
                flex-direction: column;
                height: auto;
                padding: 1rem 0.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="navbar">
        <span class="navbar-title">HUNGER STOPPERS</span>
        <div class="navbar-links">
            <a href="/food-insecurity" class="navbar-link">Learn About Food Insecurity</a>
            <a href="/model1" class="navbar-link">Model 1 Results</a>
            <a href="/model2" class="navbar-link">Model 2 Results</a>
            <a href="/about" class="navbar-link">About Us</a>
        </div>
    </div>
    <div class="main-content">
        <div class="welcome-message">
            Welcome to Hunger Stoppers
        </div>
    </div>
</body>
</html>