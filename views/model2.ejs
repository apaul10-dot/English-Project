<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Household Food Waste Prediction</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #e0f7fa 0%, #e3f2fd 100%);
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            text-align: center;
            padding: 2.5rem 2rem;
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(26, 115, 232, 0.10);
            max-width: 420px;
            width: 100%;
        }
        h1 {
            color: #1a73e8;
            margin-bottom: 2rem;
            font-size: 2rem;
            font-weight: 700;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
        }
        label {
            font-size: 1.1rem;
            color: #1565c0;
            text-align: left;
        }
        input[type="number"] {
            width: 100%;
            padding: 0.7rem;
            border: 1px solid #b3c6e0;
            border-radius: 8px;
            font-size: 1rem;
            margin-top: 0.3rem;
            background: #f7fbff;
            transition: border 0.2s;
        }
        input[type="number"]:focus {
            border: 1.5px solid #1a73e8;
            outline: none;
        }
        button[type="submit"] {
            margin-top: 1rem;
            padding: 0.9rem 0;
            font-size: 1.1rem;
            font-weight: 700;
            border: none;
            border-radius: 8px;
            background: linear-gradient(90deg, #1a73e8 0%, #34a853 100%);
            color: white;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(26, 115, 232, 0.10);
            transition: background 0.2s, transform 0.2s;
        }
        button[type="submit"]:hover {
            background: linear-gradient(90deg, #1565c0 0%, #2e7d32 100%);
            transform: translateY(-2px) scale(1.03);
        }
        .prediction {
            margin-top: 2rem;
            font-size: 1.3rem;
            color: #388e3c;
            font-weight: 600;
        }
        .back-button {
            padding: 0.5rem 1.2rem;
            font-size: 1rem;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            background: #1a73e8;
            color: white;
            text-decoration: none;
            display: inline-block;
            margin-top: 1.5rem;
            box-shadow: 0 2px 8px rgba(26, 115, 232, 0.08);
            transition: background 0.2s;
        }
        .back-button:hover {
            background: #1565c0;
        }
        @media (max-width: 600px) {
            .container {
                padding: 1.2rem 0.5rem;
            }
            h1 {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Household Food Waste Prediction</h1>
        <form method="POST" action="/model2">
            <label>Household Size:
                <input type="number" name="household_size" min="1" required>
            </label>
            <label>Income:
                <input type="number" name="income" min="0" required>
            </label>
            <label>Meals Per Day:
                <input type="number" name="meals_per_day" min="1" max="10" required>
            </label>
            <label>Food Expenditure:
                <input type="number" name="food_expenditure" min="0" required>
            </label>
            <button type="submit">Predict Food Waste</button>
        </form>
        <% if (typeof prediction !== 'undefined') { %>
            <div class="prediction">Predicted Household Food Waste: <%= prediction %> kg</div>
        <% } %>
        <a href="/" class="back-button">Back to Home</a>
    </div>
</body>
</html> 